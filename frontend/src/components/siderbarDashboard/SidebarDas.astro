<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Sidebar</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom Tailwind CSS classes */
        .sidebar-minimized {
            width: 4rem; /* equivalent to w-16 in Tailwind CSS */
        }
        .sidebar-minimized .sidebar-menu li a {
            display: none;
        }
        .sidebar-minimized .sidebar-menu li::before {
            content: attr(data-tooltip);
            color: white;
            display: block;
            text-align: center;
        }
        #sidebar {
            position: fixed;
            z-index: 1000;
        }
        #main-content {
            margin-left: 16rem; /* equivalent to w-64 in Tailwind CSS */
            transition: margin-left 0.3s;
        }
        .sidebar-minimized + #main-content {
            margin-left: 4rem; /* equivalent to w-16 in Tailwind CSS */
        }
    </style>
</head>
<body>
    <div id="sidebar" class="w-64 h-screen bg-blue-900 text-white transition-all duration-300">
        <div class="flex justify-end p-2.5 bg-gray-700">
            <button id="toggleBtn" class="bg-none border-none text-white text-2xl cursor-pointer">☰</button>
        </div>
        <ul class="sidebar-menu list-none p-0">
            <li class="p-3.5 w-full flex items-center" data-tooltip="Inicio">
                <a href="#" class="w-full text-white no-underline hover:bg-pink-500 hover:text-white">Inicio</a>
            </li>
            <li class="p-3.5 w-full flex items-center" data-tooltip="Personal">
                <a href="#" class="w-full text-white no-underline hover:bg-pink-500 hover:text-white">Personal</a>
            </li>
            <li class="p-3.5 w-full flex items-center" data-tooltip="Familiar">
                <a href="#" class="w-full text-white no-underline hover:bg-pink-500 hover:text-white">Familiar</a>
            </li>
            <li class="p-3.5 w-full flex items-center" data-tooltip="Empresarial">
                <a href="#" class="w-full text-white no-underline hover:bg-pink-500 hover:text-white">Empresarial</a>
            </li>
            <li class="p-3.5 w-full flex items-center" data-tooltip="Salir">
                <a href="#" class="w-full text-white no-underline hover:bg-pink-500 hover:text-white">Salir</a>
            </li>
        </ul>
    </div>
   

    <script>
        const toggleBtn = document.getElementById('toggleBtn');
        const sidebar = document.getElementById('sidebar');
        const mainContent = document.getElementById('main-content');
        toggleBtn.addEventListener('click', () => {
            sidebar.classList.toggle('sidebar-minimized');
            mainContent.classList.toggle('sidebar-minimized');
        });
    </script>
</body>
</html>
